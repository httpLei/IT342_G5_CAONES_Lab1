# Server Configuration
server.port=8080

# Database Configuration (Supabase PostgreSQL - Transaction Pooler)
# Using transaction pooler from Supabase dashboard
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?user=postgres.bskodipbsdigrwbclevg&password=BudTrip2026
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=budtrip2026SecretKeyForJWTTokenGenerationAndValidationPleaseKeepThisSecure
jwt.expiration=86400000

# CORS Configuration
cors.allowed.origins=http://localhost:3000,http://localhost:5173

# Logging
logging.level.com.budtrip=DEBUG
logging.level.org.springframework.security=DEBUG
