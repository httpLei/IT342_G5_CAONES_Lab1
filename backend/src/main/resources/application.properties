# Server Configuration
server.port=8080

# Database Configuration (Supabase PostgreSQL - Transaction Pooler)
# Using transaction pooler from Supabase dashboard
# prepareThreshold=0 disables prepared statements (required for pgBouncer/pooler)
spring.datasource.url=jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?user=postgres.bskodipbsdigrwbclevg&password=WorthIt2026caones&prepareThreshold=0
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true

# JWT Configuration
jwt.secret=worthit2026SecretKeyForJWTTokenGenerationAndValidationPleaseKeepThisSecure
jwt.expiration=86400000

# CORS Configuration
cors.allowed.origins=http://localhost:3000,http://localhost:5173

# Logging
logging.level.com.worthit=DEBUG
logging.level.org.springframework.security=DEBUG
